var server = require('./server_init');

server.get('/', function(req, res) {
    res.end('Hello, World!');
});

server.get('/<foo:int>', function(req, res, foo) {
    res.end('Foo: ' + foo);
});

server.listen(settings.server.port);