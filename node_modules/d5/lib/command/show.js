var fs = require('fs'),
    path = require('path'),
    _ = require('underscore');

exports = module.exports = function(args) {
    if(args.length != 1) {
        console.error('Usage: d5 show {target}');
        process.exit(1);
    }

    var target = args[0].toLowerCase();
    if(target === 'settings') {
        var contents = JSON.stringify(settings, null, 2);
        console.log(contents);
    } else if(target === 'commands') {
        var files = fs.readdirSync(__dirname);
        for(var i in files) {
            var filename = path.basename(files[i], '.js');
            if(filename !== 'show') {
                console.log(filename);
            }
        }
    } else {
        console.error('Error: unknown target "' + target + '"');
        process.exit(1);
    }
};

exports.help = function() {
    console.log('Display information.')
    console.log('Usage: d5 get {target}')
    console.log();
    console.log('Targets:');
    console.log('  settings\tshow current settings');
    console.log('  commands\tshow commands');
};
